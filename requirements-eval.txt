# Evaluation harness dependencies (development only — never enters the API image).
# RAGAS pins a coherent LangChain stack; newer langchain-community drops a module
# RAGAS imports, so these are pinned together.
ragas==0.2.14
langchain==0.3.7
langchain-core==0.3.21
langchain-community==0.3.7
langchain-openai==0.2.9
httpx==0.28.1
pyyaml==6.0.2
